import { StyleSheet } from "react-native";

const stylePersonal = StyleSheet.create({
    personalPage: {
        backgroundColor: '#7fffd4',
        alignContent: 'center',
        height: '70%'
    },
    titleText: {
        fontSize: 30,
        color: '#15317E',
        textAlign: 'center'
    }

});
export default stylePersonal;